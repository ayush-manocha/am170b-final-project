[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "am170b-final-project"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pydmd",
    "matplotlib",
    "numpy",
    "scipy",
]
